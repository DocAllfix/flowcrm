/**
 * L'anteprima interattiva a schede (Dashboard · Pipeline · Commessa · Portale agente).
 *
 * ⚠️ SPENTA DI PROPOSITO (24/09/2026). Le schermate scattate dalla demo di produzione
 * mostravano dati di test lasciati dalle suite e2e («E2E Kanban 1784…»), il tour
 * guidato aperto, il nome tecnico FlowCRM nei colori di default e un utente
 * «Verifica Admin». Una vetrina così vende il contrario di quello che dice la pagina.
 *
 * Si riaccende quando esiste un'istanza pulita con il marchio PMIFlow: si scatta con
 * `scripts/scatta-schermate.mjs`, si GUARDANO le immagini, poi si scrive qui il
 * componente a schede (isola client, `role="tablist"`, frecce da tastiera).
 */
export function Anteprima() {
  return null;
}
